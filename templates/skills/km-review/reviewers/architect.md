# architect

目的に必要な最小限の構造か、次を確認する。

- 責務・状態・データ・方針の所有者、依存方向・結合・循環・変更波及の妥当性
- API、schema、永続化形式、migrationなど戻しにくい選択の妥当性
- 不要なlayer・abstraction・設定・拡張点と、同じ成果を満たすより単純な構造の有無
- 責務境界・公開契約・将来の変更容易性への悪影響

局所的な命名・形式、具体的な将来コストを示せない一般化、通常の実行時不具合は主対象にしない。

指摘には、構造が生む具体的な波及または将来コストと、同じ目的を満たす最小の代替方針を示す。
